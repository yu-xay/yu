[在线测试](http://php.jsrun.net/)

### 可补充(5天删除)
```
debug_zval_dump('encode');                              引用次数

base64_encode(string $data):string                      使用MIME base64编码数据 2day

php_sapi_name(void):string                              返回Web服务器和PHP之间的接口类型 2day

http_build_query(array)                                 http头部数组转字符串 1day

PHP_URL_SCHEME PHP_URL_HOST PHP_URL_PORT PHP_URL_USER 
PHP_URL_PASS PHP_URL_PATH PHP_URL_QUERY PHP_URL_FRAGMENT  
parse_url(string $url, [int $component = -1]):mixed     解析 URL 2day


realpath(string $path): string                          返回规范化的绝对路径名 1day

getallheaders(void): array                              获取全部 HTTP 请求头信息 2day

array_fill(index,number,value);                         填充数组 2day

usort($array,($current,$next) => {$current <=> $next})  数组自定义排序 (比较函数须返回整数) 2day

array_multisort()
[

array_slice(array $arr, int $offset [,int $length = null]): array 从数组中取出一段 1day

array_splice(array &$arr, int $offset [, int length = count($arr)[, $reple]] : array 去掉数组莫一部分并用其他值取代 1day

wordwrap()                                        文本换行 3day


substr_count($haystack,$needle);               字符串出现次数
substr_compare("abcde", "bc", 1, 2);              文本偏移量比较 3day


substr($string.$start,$length)                 字符串子传
substr_replace($string,$replact,$start,$length) 根据位置替换 混合参数
str_replace($search, $replace,$string,$count)  子字符串替换 遍历 替换 混合参数


preg_replace(mixed $pattern, $replacement, $string) 正则替换



```

[php特性](https://xiaoxiami.gitbook.io/php-7/php-71x-xin-te-xing/xin-te-xing/ke-wei-kong-ff08-nullable-ff09-lei-xing)





