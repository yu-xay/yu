[在线测试](http://www.dooccn.com/php7/)
### 可补充(5天删除)
```
xdebug_debug_zval('encode');                            引用次数
debug_zval_dump('encode');                              引用次数

base64_encode(string $data):string                      使用MIME base64编码数据 1day

php_sapi_name(void):string                              返回Web服务器和PHP之间的接口类型 1day

http_build_query(array)                                 http头部数组转字符串

PHP_URL_SCHEME PHP_URL_HOST PHP_URL_PORT PHP_URL_USER 
PHP_URL_PASS PHP_URL_PATH PHP_URL_QUERY PHP_URL_FRAGMENT  
parse_url(string $url, [int $component = -1]):mixed     解析 URL 1day


realpath(string $path): string                          返回规范化的绝对路径名

getallheaders(void): array                              获取全部 HTTP 请求头信息 1day

array_fill(index,number,value);                         填充数组 1day

usort($array,($current,$next) => {$current <=> $next})  数组自定义排序 (比较函数须返回整数) 1day

array_multisort()

substr_replace("Hello","world",0);
substr(string $string,int $start [,int $length]):string 获取字串 1day
                                     子字符串替换
str_replace(search replace subject count)文档             子字符串替换

array_slice(array $arr, int $offset [,int $length = null]): array 从数组中取出一段

array_splice(array &$arr, int $offset [, int length = count($arr)[, $reple]] : array 去掉数组莫一部分并用其他值取代

wordwrap()   

substr_compare("abcde", "bc", 1, 2); 2day
```





